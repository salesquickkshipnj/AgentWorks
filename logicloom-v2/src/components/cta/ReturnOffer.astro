---
interface Props {
  industry?: string;
}

const { industry = "this industry" } = Astro.props;
---

<div class="bg-red-50/50 border border-red-100 rounded-2xl p-8 md:p-10 my-16">
  <div class="md:flex items-start gap-6">
    <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center shadow-sm text-red-500 shrink-0 mb-4 md:mb-0">
      <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z" /></svg>
    </div>
    
    <div>
      <h3 class="font-display text-xl font-bold text-slate-900 mb-2">
        Why automation in {industry} usually fails
      </h3>
      <p class="text-gray-600 text-sm leading-relaxed mb-6">
        Most agencies build "happy path" automations that break when an API hits a rate limit or a customer enters invalid data. You don't need a new tool—you need error handling.
      </p>
      
      <a 
        href={`/audit?context=${industry}&symptom=Reliability+Check`}
        class="inline-flex items-center gap-2 text-sm font-bold text-red-600 hover:text-red-700 transition-colors"
      >
        Check your system's reliability score &rarr;
      </a>
    </div>
  </div>
</div>