---
const testimonials = [
  {
    quote: "Our old process was a mess of spreadsheets and missed calls. LogicLoom came in, audited our workflow, and built a system that feels like magic. Professional, fast, and robust.",
    author: "Director of Operations",
    role: "Gold Remodels Inc.",
    metric: "100% Data Accuracy",
    color: "bg-blue-50 text-blue-600"
  },
  {
    quote: "We were drowning in Zillow leads. LogicLoom built an agent that qualifies them instantly. We've doubled our showing rate in 30 days.",
    author: "Sarah Jenkins",
    role: "Broker, Apex Realty",
    metric: "2x Showings",
    color: "bg-emerald-50 text-emerald-600"
  },
  {
    quote: "The automated intake forms sync directly to Clio. No more data entry errors. My paralegals are actually doing legal work now.",
    author: "David Chen",
    role: "Partner, Chen Law Group",
    metric: "Zero Data Entry",
    color: "bg-amber-50 text-amber-600"
  }
];
---

<section class="py-24 bg-gray-50 border-t border-gray-200">
  <div class="max-w-6xl mx-auto px-6">
    <div class="text-center mb-16">
      <h2 class="font-display text-3xl md:text-4xl font-bold tracking-tight mb-4 text-slate-900">
        Don't just take our word for it.
      </h2>
      <p class="text-gray-500 max-w-2xl mx-auto">
        We build systems that pay for themselves. Here is the impact on real businesses.
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
      {testimonials.map((t) => (
        <div class="bg-white p-8 rounded-2xl border border-gray-100 shadow-sm hover:shadow-lg transition-shadow duration-300 flex flex-col justify-between h-full">
          
          <div>
            <div class={"inline-block px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wide mb-6 " + t.color}>
              {t.metric}
            </div>
            <p class="text-gray-600 leading-relaxed mb-6 italic">"{t.quote}"</p>
          </div>

          <div class="flex items-center gap-4 border-t border-gray-50 pt-6">
            <div class="w-10 h-10 rounded-full bg-slate-100 flex items-center justify-center text-slate-400 font-bold">
              {t.author.charAt(0)}
            </div>
            <div>
              <div class="font-bold text-slate-900 text-sm">{t.author}</div>
              <div class="text-xs text-gray-500">{t.role}</div>
            </div>
          </div>

        </div>
      ))}
    </div>
  </div>
</section>
