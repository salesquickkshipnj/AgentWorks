---
import Layout from '../layouts/Layout.astro';
import RoiCalculator from '../components/tools/RoiCalculator.astro';
import WebhookForm from '../components/forms/WebhookForm.astro';
import TrustLogos from '../components/sections/TrustLogos.astro';
import Testimonials from '../components/sections/Testimonials.astro';
---

<Layout 
  title="LogicLoom Systems | AI Automation & Workforce Architecture"
  description="We build the AI workforce that never sleeps. Enterprise-grade n8n workflows and autonomous agents."
>
  <section id="top" class="relative pt-40 pb-16 md:pt-48 md:pb-20 px-6 overflow-hidden z-10">
    <div class="max-w-5xl mx-auto text-center relative z-10">
      
      <div class="inline-flex items-center gap-2 mb-8 px-4 py-1.5 rounded-full border border-gray-200 bg-white/60 backdrop-blur-sm shadow-sm animate-fade-in">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
        </span>
        <span class="text-xs font-semibold text-gray-600 uppercase tracking-widest">Accepting New Builds for Q1 2026</span>
      </div>

      <h1 class="font-display text-5xl md:text-7xl font-bold tracking-tight text-slate-900 mb-6 leading-[1.05] animate-slide-up">
        We Build the AI Workforce <br />
        <span class="text-gray-400">That Never Sleeps.</span>
      </h1>

      <p class="text-lg md:text-xl text-gray-500 font-normal max-w-2xl mx-auto mb-10 leading-relaxed animate-slide-up" style="animation-delay: 0.1s;">
        Stop hiring for repetitive tasks.
        We architect self-hosted <span class="text-slate-900 font-medium">n8n workflows</span> and custom agents that run your operations securely, 24/7.
      </p>

      <div class="flex flex-col md:flex-row items-center justify-center gap-4 animate-slide-up" style="animation-delay: 0.2s;">
        <a href="#contact"
          class="bg-[#0071E3] text-white text-base px-8 py-4 rounded-full font-medium hover:bg-blue-600 transition-all shadow-xl shadow-blue-500/20 active:scale-95 flex items-center gap-2 w-full md:w-auto justify-center"
        >
          Analyze My Workflow
          <i class="ph ph-arrow-down"></i>
        </a>
        
        <button
           @click="$dispatch('open-modal')"
           class="bg-white text-slate-700 border border-gray-200 text-base px-8 py-4 rounded-full font-medium hover:bg-gray-50 transition-all active:scale-95 w-full md:w-auto cursor-pointer"
        >
          Book Discovery
        </button>
      </div>

      <div class="mt-16 pt-8 border-t border-gray-200/60 animate-slide-up" style="animation-delay: 0.3s;">
        <p class="text-xs font-semibold text-gray-400 uppercase tracking-widest mb-6">Enterprise-Grade Stack</p>
        <div class="flex flex-wrap justify-center items-center gap-8 md:gap-14 opacity-50 grayscale hover:grayscale-0 transition-all duration-700">
           <div class="flex items-center gap-2 font-display font-bold text-xl text-slate-700"><i class="ph ph-lightning-fill text-orange-500"></i> n8n</div>
           <div class="flex items-center gap-2 font-display font-bold text-xl text-slate-700"><i class="ph ph-brain-fill text-green-600"></i> OpenAI</div>
           <div class="flex items-center gap-2 font-display font-bold text-xl text-slate-700"><i class="ph ph-code text-yellow-500"></i> Python</div>
           <div class="flex items-center gap-2 font-display font-bold text-xl text-slate-700"><i class="ph ph-database text-blue-600"></i> PostgreSQL</div>
           <div class="flex items-center gap-2 font-display font-bold text-xl text-slate-700"><i class="ph ph-stripe-logo text-purple-600"></i> Stripe</div>
        </div>
      </div>
    </div>
    
    <div class="absolute top-0 right-0 -z-10 w-[600px] h-[600px] bg-blue-50/50 rounded-full blur-3xl opacity-50 pointer-events-none"></div>
  </section>

  <TrustLogos />

  <section id="capabilities" class="py-24 bg-white relative z-10 border-y border-gray-100">
    <div class="max-w-6xl mx-auto px-6">
      <div class="text-center mb-20">
        <h2 class="font-display text-3xl md:text-4xl font-bold tracking-tight mb-4 text-slate-900">The Automation Infrastructure</h2>
        <p class="text-gray-500 max-w-2xl mx-auto">
          We don't just "zap" things.
          We build resilient software architecture that handles thousands of operations without breaking.
        </p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <div class="md:col-span-2 bg-gray-50 rounded-2xl p-10 relative overflow-hidden group hover:shadow-lg transition-all duration-500 border border-gray-100">
          <div class="relative z-10">
            <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center mb-6 shadow-sm border border-gray-100">
              <i class="ph ph-users-three text-2xl text-[#0071E3]"></i>
            </div>
            <h3 class="font-display text-2xl font-bold mb-3">Customer Ops Agents</h3>
            <p class="text-gray-600 max-w-md leading-relaxed">
              Deploy AI agents that triage support tickets, draft email replies, update CRM records, and route complex queries to humans only when necessary.
            </p>
            <div class="mt-8 flex gap-3">
                <span class="text-xs font-mono bg-white border border-gray-200 px-2 py-1 rounded text-gray-500">GPT-4o</span>
                <span class="text-xs font-mono bg-white border border-gray-200 px-2 py-1 rounded text-gray-500">Anthropic</span>
            </div>
          </div>
          <div class="absolute right-[-20px] bottom-[-20px] w-64 h-48 bg-white rounded-xl border border-gray-200 shadow-sm p-4 flex flex-col gap-3 opacity-90 group-hover:translate-y-[-5px] transition-transform duration-500">
             <div class="flex items-center gap-2 border-b border-gray-100 pb-2">
                 <div class="w-2 h-2 rounded-full bg-green-500"></div>
                 <span class="text-[10px] text-gray-400 font-mono">Incoming Ticket #9201</span>
             </div>
             <div class="space-y-2">
                 <div class="bg-gray-100 h-2 w-full rounded-full"></div>
                 <div class="bg-blue-50 h-2 w-2/3 rounded-full"></div>
             </div>
          </div>
        </div>

        <div class="bg-gray-50 rounded-2xl p-10 hover:shadow-lg transition-all duration-500 border border-gray-100">
          <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center mb-6 shadow-sm border border-gray-100">
            <i class="ph ph-chart-polar text-2xl text-slate-800"></i>
          </div>
          <h3 class="font-display text-xl font-bold mb-3">Data Orchestration</h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Unify data from fragmented tools.
            We build pipelines that pull from APIs, clean the data in Python/JS, and push to your dashboard or warehouse.
          </p>
        </div>

        <div class="bg-gray-50 rounded-2xl p-10 hover:shadow-lg transition-all duration-500 border border-gray-100">
          <div class="w-12 h-12 bg-white rounded-xl flex items-center justify-center mb-6 shadow-sm border border-gray-100">
            <i class="ph ph-plugs text-2xl text-purple-600"></i>
          </div>
          <h3 class="font-display text-xl font-bold mb-3">Custom Integrations</h3>
          <p class="text-gray-600 text-sm leading-relaxed">
            Internal tool doesn't have a Zapier app? No problem.
            We write custom API wrappers and webhooks to connect the disconnectable.
          </p>
        </div>

        <div class="md:col-span-2 bg-slate-900 text-white rounded-2xl p-10 relative overflow-hidden group">
          <div class="relative z-10 flex flex-col md:flex-row md:items-center justify-between gap-6">
            <div class="max-w-lg">
                <h3 class="font-display text-2xl font-bold mb-2">The Architecture Audit</h3>
                <p class="text-gray-400">
                  Not sure what's possible?
                  We map your current stack, identify high-friction bottlenecks, and calculate the ROI of automation before writing a single line of code.
                </p>
            </div>
            <button
              @click="$dispatch('open-modal')"
              class="whitespace-nowrap bg-white text-slate-900 text-sm font-semibold rounded-full px-6 py-3 hover:bg-gray-100 transition-colors cursor-pointer"
            >
              Start Audit Strategy
            </button>
          </div>
        </div>
      </div>
    </div>
  </section>

  <Testimonials />

  <section id="process" class="py-24 relative overflow-hidden bg-gray-50 z-10">
    <div class="max-w-6xl mx-auto px-6 relative z-10">
      <div class="mb-16 md:text-center max-w-2xl mx-auto">
        <h2 class="font-display text-3xl font-bold tracking-tight mb-4">The LogicLoom Protocol</h2>
        <p class="text-gray-500">A rigorous engineering approach to business process automation.</p>
      </div>

      <div class="grid grid-cols-1 md:grid-cols-3 gap-12 relative">
        <div class="hidden md:block absolute top-8 left-0 w-full h-px bg-gradient-to-r from-transparent via-gray-300 to-transparent"></div>
        
        <div class="relative group">
          <div class="w-16 h-16 bg-white border border-gray-200 rounded-2xl flex items-center justify-center text-xl font-bold shadow-sm mb-6 relative z-10 mx-auto md:mx-0 group-hover:border-blue-500 group-hover:text-blue-500 transition-colors">
              <span class="font-display">01</span>
          </div>
          <h3 class="font-display text-lg font-bold mb-2">Map & Measure</h3>
          <p class="text-sm text-gray-500 leading-relaxed">
            We audit your manual workflows step-by-step.
            We don't just ask "what do you do?"—we ask "why?" and identify where data breaks.
          </p>
        </div>

        <div class="relative group">
          <div class="w-16 h-16 bg-white border border-gray-200 rounded-2xl flex items-center justify-center text-xl font-bold shadow-sm mb-6 relative z-10 mx-auto md:mx-0 group-hover:border-blue-500 group-hover:text-blue-500 transition-colors">
              <span class="font-display">02</span>
          </div>
          <h3 class="font-display text-lg font-bold mb-2">Architect & Build</h3>
          <p class="text-sm text-gray-500 leading-relaxed">
            I build your agents using n8n and code.
            We implement error handling, logging, and fallback paths so your business doesn't crash on a bad API call.
          </p>
        </div>

        <div class="relative group">
          <div class="w-16 h-16 bg-white border border-gray-200 rounded-2xl flex items-center justify-center text-xl font-bold shadow-sm mb-6 relative z-10 mx-auto md:mx-0 group-hover:border-blue-500 group-hover:text-blue-500 transition-colors">
              <span class="font-display">03</span>
          </div>
          <h3 class="font-display text-lg font-bold mb-2">Deploy & Transfer</h3>
          <p class="text-sm text-gray-500 leading-relaxed">
            We deploy to your private server or cloud.
            You get full documentation and ownership of the source code. No vendor lock-in.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section id="contact" class="py-24 bg-white border-t border-gray-200 relative z-10">
    <div class="max-w-6xl mx-auto px-6">
      
      <div class="mb-20">
        <div class="text-center mb-10">
            <h2 class="font-display text-3xl font-bold tracking-tight mb-2">The Cost of Doing Nothing</h2>
            <p class="text-gray-500 text-sm">Use our calculator to see how much manual work is actually costing you.</p>
        </div>
        <RoiCalculator />
      </div>

      <div class="grid md:grid-cols-2 gap-16 items-start mt-24 border-t border-gray-100 pt-20">
        <div>
            <h2 class="font-display text-3xl font-bold tracking-tight mb-6">Technical Feasibility Audit</h2>
            <p class="text-gray-500 mb-8 leading-relaxed">
                Tell us about your process. We will review your requirements and tell you:
            </p>
            <ul class="space-y-4 mb-8">
                <li class="flex items-center gap-3 text-sm text-gray-600">
                    <i class="ph ph-check-circle text-green-500 text-xl"></i>
                    If it can be automated reliably.
                </li>
                <li class="flex items-center gap-3 text-sm text-gray-600">
                    <i class="ph ph-check-circle text-green-500 text-xl"></i>
                    Which tools (n8n, Python, AI) are best suited.
                </li>
                <li class="flex items-center gap-3 text-sm text-gray-600">
                    <i class="ph ph-check-circle text-green-500 text-xl"></i>
                    Estimated development timeline and cost.
                </li>
            </ul>
        </div>

        <div class="bg-gray-50 p-8 rounded-3xl border border-gray-200">
            <WebhookForm 
                webhookUrl="https://flexaiautomations.app.n8n.cloud/webhook/audit-request" 
                formId="audit-form" 
            />
        </div>
      </div>

    </div>
  </section>

  <style>
    /* Subtle float animation for hero image */
    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0px); }
    }
    .animate-float {
      animation: float 6s ease-in-out infinite;
    }
    .animate-fade-in { animation: fadeIn 0.6s ease-out forwards; }
    .animate-slide-up { animation: slideUp 0.8s ease-out forwards; }
    @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    @keyframes slideUp { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }
  </style>
</Layout>