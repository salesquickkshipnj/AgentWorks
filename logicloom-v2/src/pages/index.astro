---
import Layout from '../layouts/Layout.astro';
---

<Layout 
  title="LogicLoom Systems | AI Automation & Workforce Architecture"
  description="We build the AI workforce that never sleeps. Enterprise-grade n8n workflows and autonomous agents."
>
  <div x-data="{ modalOpen: false }">

    <section class="relative pt-32 pb-20 px-6 overflow-hidden">
      <div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-12 items-center">
        
        <div class="relative z-10 text-center lg:text-left">
          <div class="inline-flex items-center gap-2 mb-6 px-3 py-1 rounded-full border border-blue-100 bg-blue-50 text-blue-700">
             <span class="relative flex h-2 w-2">
               <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-blue-400 opacity-75"></span>
               <span class="relative inline-flex rounded-full h-2 w-2 bg-blue-500"></span>
             </span>
             <span class="text-xs font-bold uppercase tracking-widest">Accepting New Builds for Q1 2026</span>
          </div>
          
          <h1 class="font-display text-5xl md:text-7xl font-bold text-slate-900 leading-[1.1] mb-6">
            We Build the <br /> 
            <span class="text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-indigo-600">AI Workforce</span> <br />
            That Never Sleeps.
          </h1>
          
          <p class="text-xl text-gray-500 mb-8 max-w-lg mx-auto lg:mx-0 leading-relaxed">
            Stop hiring for repetitive tasks. We architect self-hosted n8n workflows and custom agents that run your operations securely, 24/7.
          </p>
          
          <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
            <button @click="modalOpen = true" class="bg-slate-900 text-white px-8 py-4 rounded-full font-bold hover:bg-slate-800 transition-all shadow-xl shadow-slate-900/20">
              Analyze My Workflow
            </button>
            <a href="/n8n-services" class="px-8 py-4 rounded-full font-bold border border-gray-200 hover:bg-gray-50 text-slate-700 transition-all text-center">
              View Services
            </a>
          </div>
        </div>

        <div class="relative z-10 flex justify-center lg:justify-end">
           <img 
             src="/LogicLoom_Neural.svg" 
             alt="LogicLoom Neural Network" 
             class="w-full max-w-[600px] h-auto object-contain drop-shadow-2xl animate-float"
           />
        </div>
      </div>
      
      <div class="absolute top-0 right-0 -z-10 w-[600px] h-[600px] bg-blue-50/50 rounded-full blur-3xl opacity-50 pointer-events-none"></div>
    </section>

    <section class="py-10 border-y border-gray-100 bg-slate-50/50">
       <div class="max-w-6xl mx-auto px-6 text-center">
          <p class="text-xs font-bold text-gray-400 uppercase tracking-widest mb-6">Powering Automation For</p>
          <div class="flex flex-wrap justify-center gap-12 grayscale opacity-50">
             <i class="ph ph-house-line text-3xl"></i>
             <i class="ph ph-gavel text-3xl"></i>
             <i class="ph ph-tooth text-3xl"></i>
             <i class="ph ph-lightning text-3xl"></i>
          </div>
       </div>
    </section>

    <div x-show="modalOpen" style="display:none;" class="fixed inset-0 z-[100] flex items-center justify-center px-4" x-transition.opacity>
      <div @click="modalOpen = false" class="absolute inset-0 bg-slate-900/60 backdrop-blur-sm"></div>
      <div class="relative bg-white w-full max-w-lg rounded-2xl shadow-2xl overflow-hidden">
        <div class="flex justify-between items-center p-4 border-b border-gray-100">
          <h3 class="font-bold text-lg">Book Discovery Call</h3>
          <button @click="modalOpen = false" class="text-gray-400 hover:text-black"><i class="ph ph-x text-xl"></i></button>
        </div>
        <div class="h-[500px] w-full bg-white">
           <iframe src="https://cal.com/logicloomsystems-cal/15min?embed=true" style="width:100%;height:100%;border:none;"></iframe>
        </div>
      </div>
    </div>

  </div>
  
  <style>
    /* Subtle float animation for the hero image */
    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
      100% { transform: translateY(0px); }
    }
    .animate-float {
      animation: float 6s ease-in-out infinite;
    }
  </style>
</Layout>